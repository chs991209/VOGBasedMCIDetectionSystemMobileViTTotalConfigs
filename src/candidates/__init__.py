"""Pipeline candidates — the full catalog of architectural approaches evaluated
during the VOG-MCI project.

Each sub-package holds ONE candidate architecture end-to-end. Code redundancy
across candidates is intentional: each candidate should be readable in
isolation, without needing to trace shared abstractions across the project.

For the top-level index and status table, see `README.md`.
"""
